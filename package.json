{
  "name": "autoservicemanager-auth-lambda",
  "version": "0.1.0",
  "private": true,
  "description": "Lambda authCpf — JWT RS256 por CPF (ADR-007). Repo alvo: autoservicemanager-auth-lambda.",
  "license": "UNLICENSED",
  "main": "dist/handler.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "test": "jest --config jest.config.cjs",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "package": "yarn build && cd dist && zip -r ../auth-cpf.zip handler.js"
  },
  "dependencies": {
    "@autoservicemanager/domain-shared": "file:../packages/domain-shared",
    "@aws-sdk/client-secrets-manager": "^3.787.0",
    "jsonwebtoken": "^9.0.3",
    "mysql2": "^3.14.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "@types/jest": "^30.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^22.10.7",
    "esbuild": "^0.25.2",
    "jest": "^30.0.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.7.3"
  }
}
